# My Linux Scripts

A collection of personal utility scripts for my Fedora setup.

## Scripts
- **[project.sh](project.sh)**: A launcher for quickly opening directories in `zed` and `ghostty` using `zoxide`.

## Installation
Clone this repository to your local machine:

```bash
git clone https://github.com/evilsam-2011/scripts.git
